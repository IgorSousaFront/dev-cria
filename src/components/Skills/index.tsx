import { Container } from "@/components/Container"
import { SectionTitle } from "../Title"

export const Skills = () => {
  return (
    <section className="w-[98vw] py-11 mx-auto bg-white shadow-lg">
      <Container>
        <SectionTitle color="primary">Habilidades</SectionTitle>
      </Container>
    </section>
  )
}